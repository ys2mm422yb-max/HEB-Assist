{
  "name": "heb-assist",
  "private": true,
  "type": "module",
  "dependencies": {
    "@huggingface/transformers": "4.2.0"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  },
  "scripts": {
    "build:runtime": "mkdir -p vendor && cp node_modules/@huggingface/transformers/dist/transformers.web.min.js vendor/transformers.js && cp node_modules/onnxruntime-web/dist/ort-wasm-simd-threaded.jsep.mjs vendor/ && cp node_modules/onnxruntime-web/dist/ort-wasm-simd-threaded.jsep.wasm vendor/",
    "test:browser": "playwright test"
  }
}
